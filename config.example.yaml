traffic:
  decimals: 2
  binary_units: false
  unlimited: "∞"
  # Hide the quota from the client's own traffic display (sends total=0)
  force_unlimited: false
  # units: ["Б", "КБ", "МБ", "ГБ", "ТБ", "ПБ", "ЭБ"]

# Layouts use Go reference time: 02.01.2006 15:04 is day.month.year hour:minute.
datetime:
  layout: "02.01.2006"
  time_layout: "15:04"
  timezone: "UTC"
  never: "∞"

progress_bar:
  width: 10
  filled: "▰"
  empty: "▱"

template:
  # keep | blank
  unknown: keep
  scan_all_headers: true
  decode_base64: true

# Custom placeholders. Names must be UPPER_SNAKE_CASE.
vars:
  BRAND: "MyProject"
  SUPPORT: "@my_support_bot"

headers:
  - name: announce
    template: "{BRAND} · {TRAFFIC_USED} of {TRAFFIC_LIMIT} used · {DAYS_LEFT} days left · {SUPPORT}"
    # auto | none | base64 | base64-prefixed
    encode: base64-prefixed
    max_length: 200

  # - name: announce
  #   template: "{BRAND} · traffic limit reached · renew at {SUPPORT}"
  #   encode: base64-prefixed
  #   when:
  #     user_statuses: [LIMITED, EXPIRED]

  # - name: profile-title
  #   template: "{BRAND} — {TRAFFIC_AVAILABLE}"
  #   encode: base64
  #   max_length: 25

  # - name: announce
  #   template: "{PROGRESS_BAR} {TRAFFIC_USED_PERCENT}%"
  #   encode: base64-prefixed
  #   when:
  #     user_agent: "(?i)happ"

  # - name: X-Plan-Summary
  #   template: "{TRAFFIC_USED} / {TRAFFIC_LIMIT}"
  #   when:
  #     client_types: [clash, json]

  # - name: announce
  #   template: "{BRAND} · {TRAFFIC_USED} of {TRAFFIC_LIMIT} used"
  #   encode: base64-prefixed
  #   when:
  #     has_traffic_limit: true

  # - name: announce
  #   template: "{BRAND} · unlimited traffic"
  #   encode: base64-prefixed
  #   when:
  #     has_traffic_limit: false

  # - name: support-url
  #   template: "https://t.me/my_support_bot"
  #   when:
  #     exists: false

  # - name: profile-web-page-url
  #   remove: true
